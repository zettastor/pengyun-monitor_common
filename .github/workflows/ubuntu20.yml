name: pengyun-monitor_common
on: 
  workflow_dispatch:
  push:
    paths-ignore:
      - '.gitignore'
      - '.github/workflows/**.yml'

jobs:
  build:
    uses: zettastor/pengyun-root/.github/workflows/ubuntu20.yml@1.0-OS
